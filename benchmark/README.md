# Cross-language benchmarks

Performance comparisons between R, C++, and Python implementations on the same
inputs. Used to validate that the C++ core delivers expected speedups and that
Python and R bindings stay close to native C++ performance.
