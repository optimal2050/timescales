% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\name{ALIGNMENT_RULES}
\alias{ALIGNMENT_RULES}
\title{Supported alignment rules}
\format{
A character vector of length 4.
}
\usage{
ALIGNMENT_RULES
}
\description{
Alignment declares how real Gregorian instants that fall outside a
calendar's vocabulary map onto it — a separate axis from aggregation
(conflating the two is how a non-conserving \code{sum} arises). Resurrects the
\code{alignment_rule} vocabulary of the predecessor \code{timeslices} package.
}
\details{
\describe{
\item{\code{exact}}{Error if any instant falls outside the vocabulary.}
\item{\code{drop_last}}{Instants past the last label map to \code{NA} (e.g. the
trailing days of the year for \code{d360}).}
\item{\code{drop_feb29}}{Feb 29 maps to \code{NA}; later ydays shift down by one,
so Dec 31 of a leap year is still \code{d365}.}
\item{\code{repeat_last}}{Instants past the last label clamp to it (e.g.
week 53 folds into \code{w52}).}
}

Alignment lives per-timeframe in a calendar's \code{meta$alignment} (a named
list), seeded by the tokens that built it and overridable at construction
or in \code{\link[=datetime_to_timeslice]{datetime_to_timeslice()}}. Unaligned out-of-vocabulary grid points map to
\code{NA}, surfaced by \code{recast_calendar()}'s \code{na_action}.
}
\examples{
ALIGNMENT_RULES
}
