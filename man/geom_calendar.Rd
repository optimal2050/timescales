% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot-layers.R
\name{geom_calendar}
\alias{geom_calendar}
\alias{geom_calendar_tile}
\alias{theme_calendar}
\title{Calendar layers for ggplot2}
\usage{
geom_calendar(
  calendar,
  z,
  datetime = "datetime",
  by = NULL,
  x_tf = NULL,
  y_tf = NULL,
  fun = mean,
  data = NULL,
  ...
)

geom_calendar_tile(
  calendar,
  z,
  timeslice = "timeslice",
  by = NULL,
  x_tf = NULL,
  y_tf = NULL,
  fun = mean,
  data = NULL,
  ...
)

theme_calendar(...)
}
\arguments{
\item{calendar}{A \code{\link{Calendar}}.}

\item{z}{Name of the numeric column to aggregate and fill by.}

\item{datetime}{Name of the POSIXct/Date column (\code{geom_calendar()}).
Default \code{"datetime"}.}

\item{by}{Character vector of columns to preserve through aggregation
(facet/group carriers). Default none.}

\item{x_tf, y_tf}{Timeframes for the x and y axes. Default: finest on y,
next-finest on x.}

\item{fun}{Aggregator over instants/timeslices falling in one tile. Default
\code{mean}.}

\item{data}{A \code{data.frame}; \code{NULL} (default) uses the plot data.}

\item{...}{Passed to the tile geom (e.g. \code{colour}, \code{linewidth}), or for
\code{theme_calendar()} to \code{\link[ggplot2:theme]{ggplot2::theme()}}.}

\item{timeslice}{Name of the timeslice-ID column (\code{geom_calendar_tile()}).
Default \code{"timeslice"}.}
}
\value{
A single ggplot2 layer (\code{theme_calendar()} returns a theme).
}
\description{
Composable single layers that put time-series data on a calendar inside
a normal \code{ggplot()} pipeline (the assembled-figure counterparts are
\code{\link[=calendar_autoplot]{calendar_autoplot()}} and \code{\link[=calendar_plot]{calendar_plot()}}):
}
\details{
\itemize{
\item \code{geom_calendar()} — \strong{datetime mode}: name a POSIXct/Date column
(\verb{datetime=}) and a measured column (\verb{z=}); instants are cut to timeslices
via \code{\link[=instant_to_timeslice]{instant_to_timeslice()}} and aggregated with \code{fun}.
\item \code{geom_calendar_tile()} — \strong{timeslice mode}: name a timeslice-ID column
(\verb{timeslice=}) and the measured column (\verb{z=}).
\item \code{theme_calendar()} — the compact heatmap theme the assembled plots
use.
}

The calendar inputs are column \strong{names}, not \code{aes()} mappings: ggplot2
trains positional scales before statistics run, so a ggproto Stat cannot
emit the discrete axes a calendar heatmap needs. Each function instead
returns one standard tile layer whose data is derived from the plot (or
layer) data — discrete scales, facets, and themes then work through the
normal ggplot2 path. The tile fill is the aggregated \code{value}; axes are
vocabulary-ordered factors, so \code{scale_x_discrete()} etc. apply as usual.

\strong{Faceting}: list the columns your facets need in \verb{by=} — aggregation
then happens within each combination and the columns survive into the
layer data (see the example).
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)
  cal <- calendar("m12_h24")

  # datetime mode with a facet carrier
  x <- data.frame(
    t = as.POSIXct("2021-01-01", tz = "UTC") + 3600 * (0:999),
    v = rnorm(1000),
    site = rep(c("A", "B"), 500)
  )
  ggplot(x) +
    geom_calendar(calendar = cal, datetime = "t", z = "v", by = "site") +
    facet_wrap(~site) +
    theme_calendar()

  # timeslice mode
  y <- data.frame(timeslice = S7::prop(cal, "leaves")$timeslice, v = 1:288)
  ggplot(y) +
    geom_calendar_tile(calendar = cal, z = "v") +
    theme_calendar()
}
}
