% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navigate.R
\name{calendar_share}
\alias{calendar_share}
\title{Duration shares at a timeframe}
\usage{
calendar_share(calendar, timeframe, within = NULL)
}
\arguments{
\item{calendar}{A \link{Calendar}.}

\item{timeframe}{Timeframe to aggregate the shares at.}

\item{within}{Optional coarser timeframe to normalize within.}
}
\value{
A \code{data.frame} with the \code{timeframe} column (and the \code{within}
column when given) plus \code{share}.
}
\description{
Leaf shares summed to a timeframe and normalized — the time-side
mirror of \code{geoscales::geoscale_share()}. Without \code{within}, shares sum
to 1 over the whole calendar; with \code{within} (a coarser timeframe),
they sum to 1 inside each parent label.
}
\examples{
calendar_share(calendar("m12"), "MONTH")
calendar_share(calendar("q4_h24"), "HOUR", within = "QUARTER")
}
