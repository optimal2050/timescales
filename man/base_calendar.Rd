% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base-calendar.R
\name{base_calendar}
\alias{base_calendar}
\title{Enumerate the base instant grid for one or more years}
\usage{
base_calendar(years, by = "hour", tz = "UTC")
}
\arguments{
\item{years}{Integer vector of Gregorian years.}

\item{by}{Grid step, passed to \code{seq.POSIXt} (\code{"hour"}, \code{"day"},
\code{"15 min"}, ...). Default \code{"hour"}.}

\item{tz}{Time zone of the grid. Default \code{"UTC"}.}
}
\value{
A \code{data.frame} with columns \code{datetime} (POSIXct) and \code{year}
(integer, the Gregorian year the instant belongs to).
}
\description{
Returns the multi-year grid of real date-time instants that conversions
route through — the 1:1 correspondence between the package's calendars and
\code{POSIXct}. Each instant represents the interval \verb{[t, t + step)}; a leap
year at \code{by = "hour"} has 8784 rows. Results are cached per
\verb{(years, by, tz)}.
}
\examples{
nrow(base_calendar(2020))            # 8784 (leap year)
nrow(base_calendar(2021))            # 8760
nrow(base_calendar(2019:2020, by = "day"))  # 365 + 366
}
