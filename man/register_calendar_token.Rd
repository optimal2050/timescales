% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tokens.R
\name{register_calendar_token}
\alias{register_calendar_token}
\alias{get_calendar_token}
\alias{list_calendar_tokens}
\title{Register or look up a calendar token}
\usage{
register_calendar_token(name, timeframe, expand, alignment = NULL)

get_calendar_token(name)

list_calendar_tokens()
}
\arguments{
\item{name}{Character scalar — the token name (e.g. \code{"m12"}).}

\item{timeframe}{One of \code{\link{CORE_TIMEFRAMES}}.}

\item{expand}{A zero-argument function returning a \code{data.frame} with
columns \code{label} (character, unique) and \code{share} (numeric > 0, summing
to 1).}

\item{alignment}{Optional default alignment rule (one of
\code{\link{ALIGNMENT_RULES}}) declaring how real instants beyond this token's
vocabulary map onto it (e.g. \code{"drop_feb29"} for \code{d365}). Calendars built
from the token inherit it in \code{meta$alignment}.}
}
\value{
\code{register_calendar_token()} invisibly returns the token name.
\code{get_calendar_token()} returns the token definition (a list with \code{timeframe},
\code{expand}, and optionally \code{alignment}). \code{list_calendar_tokens()} returns a
character vector of all registered token names.
}
\description{
Tokens are named recipes for the labels (and within-year shares) of one
timeframe in a calendar hierarchy. A handful of built-in tokens covers the
common cases (\code{d365}, \code{m12}, \code{m12a}, \code{q4}, \code{w52}, \code{w53}, \code{wd7}, \code{h24},
\code{h168}, \code{min60}, \code{d360}, \code{d364}, \code{d366}). Custom tokens may be added with
\code{\link[=register_calendar_token]{register_calendar_token()}}.
}
\examples{
# Register a custom 4-period day-of-year partition
register_calendar_token("d4q", "YDAY", function() {
  data.frame(label = c("Q1d", "Q2d", "Q3d", "Q4d"),
             share = c(90, 91, 92, 92) / 365)
})
"d4q" \%in\% list_calendar_tokens()
}
