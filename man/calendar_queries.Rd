% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navigate.R
\name{calendar_queries}
\alias{calendar_queries}
\alias{calendar_timeframes}
\alias{calendar_rank}
\alias{calendar_timeslices}
\title{Calendar hierarchy queries}
\usage{
calendar_timeframes(calendar)

calendar_rank(calendar, timeframe)

calendar_timeslices(calendar, timeframe = NULL)
}
\arguments{
\item{calendar}{A \link{Calendar}.}

\item{timeframe}{A timeframe name (see \code{calendar_timeframes()}).}
}
\value{
\code{calendar_timeframes()} and \code{calendar_timeslices()} return a
character vector; \code{calendar_rank()} an integer.
}
\description{
Read-only queries on the timeframe hierarchy of a \link{Calendar} —
the time-side mirror of \code{geoscales::geoscale_levels()} and friends.
}
\details{
\itemize{
\item \code{calendar_timeframes()} — hierarchy names, coarsest first.
\item \code{calendar_rank()} — position of a timeframe (1 = coarsest);
\code{NA} for unknown names.
\item \code{calendar_timeslices()} — with a \code{timeframe}, the canonical ordered
labels at that level; without, the leaf \code{timeslice} ids.
}
}
\examples{
cal <- calendar("q4_h24")
calendar_timeframes(cal)
calendar_rank(cal, "HOUR")
calendar_timeslices(cal, "QUARTER")
}
