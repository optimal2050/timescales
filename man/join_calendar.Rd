% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot-layers.R
\name{join_calendar}
\alias{join_calendar}
\title{Attach a calendar's timeframe columns to timeslice-keyed data}
\usage{
join_calendar(
  x,
  calendar,
  key = "timeslice",
  timeframes = NULL,
  as_factor = TRUE
)
}
\arguments{
\item{x}{A \code{data.frame} with a column of timeslice IDs.}

\item{calendar}{A \code{\link{Calendar}}.}

\item{key}{Name of the timeslice key column in \code{x}. Default \code{"timeslice"}.}

\item{timeframes}{Which timeframe columns to attach. Default: all of the
calendar's timeframes.}

\item{as_factor}{Attach timeframe columns as vocabulary-ordered factors
(default \code{TRUE}) or plain character.}
}
\value{
\code{x} with the requested timeframe columns plus \code{share} and
\code{weight} appended. Rows whose key is not a timeslice of the calendar get
\code{NA}s (with a warning).
}
\description{
Joins the calendar's timeframe columns, \code{share}, and \code{weight} onto a
\code{data.frame} keyed by timeslice ID — the attachment step for manual ggplot2
work, faceting, or grouped summaries. Timeframe columns are added as
factors in vocabulary order so axes and facets sort correctly.
}
\examples{
cal <- calendar("m12_h24")
x <- data.frame(timeslice = S7::prop(cal, "leaves")$timeslice, load = 1)
head(join_calendar(x, cal))
}
