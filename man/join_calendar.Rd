% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{join_calendar}
\alias{join_calendar}
\title{Attach a calendar to a dataset}
\usage{
join_calendar(
  x,
  calendar,
  key = NULL,
  timeframes = NULL,
  meta = FALSE,
  as_factor = TRUE,
  year = NULL,
  by = NULL,
  tz = "UTC",
  collect = NULL
)
}
\arguments{
\item{x}{The dataset, in any supported backend (see
\code{\link[=recast_calendar]{recast_calendar()}}'s Backends section).}

\item{calendar}{A named \code{\link{Calendar}}.}

\item{key}{Key column of \code{x}: a timeslice-label column, or a POSIXct
datetime column. \code{NULL} (default) auto-detects as described above.}

\item{timeframes}{Character vector of the calendar's timeframes to
attach as \code{"<name>.<TIMEFRAME>"} columns (default: none). \code{TRUE}
attaches all of them.}

\item{meta}{Attach \code{"<name>.share"} and \code{"<name>.weight"} columns
(default \code{FALSE}).}

\item{as_factor}{Attach timeframe columns as vocabulary-ordered factors
(default \code{TRUE}) or plain character. (Lazy backends store them as
dictionary/character columns.)}

\item{year}{Model year(s) for the base grid when attaching by datetime.
Default: the span of years observed in the data, padded one year each
side.}

\item{by, tz}{Base-grid resolution and time zone for the datetime route,
as in \code{\link[=expand_calendar]{expand_calendar()}}.}

\item{collect}{For lazy inputs: materialise (\code{TRUE}) or return the
query (default).}
}
\value{
\code{x} with the new column(s) appended, in the input's class
(lazy in, lazy out).
}
\description{
Adds a timeslice-label column named after the calendar (its
\code{meta$name}), plus optionally the calendar's timeframe columns and
share/weight, all prefixed \code{"<name>."}. Because every calendar attaches
under its own name, several calendars can be joined to the same dataset
-- and a dataset carrying two label columns is a direct crosswalk
between those calendars.
}
\details{
The key is auto-detected: an existing column named like the calendar is
used as-is; else a \code{timeslice} column (labels validated against the
calendar, with a warning for unknown codes); else a \code{datetime} column
(labels computed on the base grid via \code{\link[=datetime_to_timeslice]{datetime_to_timeslice()}} --
this is how a calendar is attached to raw datetime observations).
Existing columns are never overwritten; the join errors instead.
}
\examples{
cal <- calendar("m12_h24")
x <- data.frame(timeslice = S7::prop(cal, "leaftable")$timeslice, load = 1)
head(join_calendar(x, cal))                    # adds `m12_h24`
head(join_calendar(x, cal, timeframes = TRUE)) # + m12_h24.MONTH, ...

# two calendars on one dataset = a direct crosswalk between them
xt <- data.frame(datetime = seq(as.POSIXct("2021-01-01", tz = "UTC"),
                                by = "hour", length.out = 48), v = 1)
xt <- join_calendar(xt, calendar("m12_h24"))
xt <- join_calendar(xt, calendar("q4_h24"))
head(xt)
}
