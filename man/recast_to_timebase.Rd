% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recast.R
\name{recast_to_timebase}
\alias{recast_to_timebase}
\alias{recast_from_timebase}
\title{Recast timeslice data down to the base grid, and back}
\usage{
recast_to_timebase(
  x,
  calendar,
  year = NULL,
  key = NULL,
  values = NULL,
  rule = NULL,
  by = NULL,
  tz = "UTC",
  weight = TRUE,
  collect = NULL
)

recast_from_timebase(
  x,
  calendar,
  year = NULL,
  key = NULL,
  values = NULL,
  rule = NULL,
  by = NULL,
  tz = "UTC",
  na_action = c("drop", "error", "keep"),
  collect = NULL
)
}
\arguments{
\item{x}{The data: for \code{recast_to_timebase()} keyed by timeslice (\code{key}
column, plus an optional \code{year} column for multi-year data); for
\code{recast_from_timebase()} keyed by a POSIXct \code{datetime} column.}

\item{calendar}{The \code{\link{Calendar}} the data is keyed in (\code{to_base}) or
aggregated into (\code{from_base}).}

\item{year}{Model year(s) for the grid. \code{recast_to_timebase()}: defaults to
the distinct values of \code{x$year} when present (required otherwise).
\code{recast_from_timebase()}: defaults to the span of years observed in
\code{x$datetime} (padded one year each side for year_start offsets).}

\item{key}{\code{to_base}: the timeslice key column, default \code{"timeslice"}
(falling back to a column named like the calendar). \code{from_base}: the
datetime column, default \code{"datetime"}.}

\item{values, rule, by, tz}{As in \code{\link[=recast_calendar]{recast_calendar()}}.}

\item{weight}{\code{to_base} only: attach the \code{weight} column (default
\code{TRUE}).}

\item{collect}{For lazy inputs: materialise (\code{TRUE}) or return the
query (default).}

\item{na_action}{\code{from_base} only: what to do with rows whose datetime
the calendar does not cover -- \code{"drop"} (default, warning), \code{"error"},
or \code{"keep"} (an \code{NA} timeslice row).}
}
\value{
\code{recast_to_timebase()}: one row per (grid point x identifier
combination) with columns \code{datetime}, \code{year}, identifiers, values
(and \code{weight}). \code{recast_from_timebase()}: one row per (year x timeslice x
identifier combination) with columns \code{key}-named timeslice, \code{year},
identifiers, values. Both in the input's class; lazy in, lazy out.
}
\description{
The two public halves of the \code{A -> base -> B} route:
\code{recast_to_timebase()} projects timeslice-keyed data DOWN to the base
datetime grid (one row per grid point), and \code{recast_from_timebase()}
aggregates datetime-keyed data UP into a calendar's timeslices. Their
composition is \code{\link[=recast_calendar]{recast_calendar()}}:
\code{recast_from_timebase(recast_to_timebase(x, from), to)}.
}
\details{
Going down, extensive columns (rule \code{"sum"}) are split equally across a
timeslice's grid points so totals conserve; intensive columns are
repeated. A \code{weight} column (the source timeslice's \code{share} divided by
its grid-point count) is attached by default so that the return trip's
\code{"weighted_mean"} reproduces the source calendar's weighting exactly;
pass \code{weight = FALSE} to omit it.

Going up, rules act on the grid rows directly: \code{"sum"} sums,
\code{"mean"} averages, \code{"weighted_mean"} uses the \code{weight} column when
present (else it equals \code{"mean"}), \code{"copy"} requires constancy, \code{"sd"}
is the standard deviation over the grid points.

Both ends run as dplyr pipelines and accept any supported backend (see
\code{\link[=recast_calendar]{recast_calendar()}}'s Backends section); the calendar side of every
join is a small in-memory grid.
}
\examples{
m12 <- calendar_build("m12")
x <- data.frame(timeslice = sprintf("m\%02d", 1:12), energy = 1:12)
g <- recast_to_timebase(x, m12, year = 2021, rule = "sum", by = "day")
head(g)
sum(g$energy)  # 78 -- totals conserve

q4 <- calendar_build("q4")
recast_from_timebase(g, q4, rule = "sum", by = "day")
}
