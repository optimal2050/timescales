% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wall.R
\name{calendar_wall_plot}
\alias{calendar_wall_plot}
\title{Wall-calendar figure}
\usage{
calendar_wall_plot(
  x,
  data = NULL,
  z = NULL,
  year = NULL,
  arrange = c("weekday", "sequence"),
  week_start = "MON",
  fun = mean,
  label = TRUE,
  key = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{\link{Calendar}} with a day-resolution timeframe
(\code{YDAY} or \code{MDAY}). Sub-daily timeframes are collapsed to the day
layer via \code{\link[=prune_calendar]{prune_calendar()}}.}

\item{data}{Optional \code{data.frame} of values to fill the day cells.}

\item{z}{Name of the numeric value column of \code{data}.}

\item{year}{Integer model year (required for the weekday
arrangement; \code{NULL} falls back to \code{"sequence"} with a message).}

\item{arrange}{\code{"weekday"} or \code{"sequence"} (see above).}

\item{week_start}{Weekday the week starts on: one of
\code{"MON"}..\code{"SUN"} (default \code{"MON"}, ISO).}

\item{fun}{Aggregator collapsing multiple observations (sub-daily
timeslices, repeated dates) into one day cell. Default \code{mean}.}

\item{label}{Draw day-of-month numbers (default \code{TRUE}).}

\item{key}{Key column of \code{data}; \code{NULL} auto-detects.}

\item{...}{Passed to \code{\link[ggplot2:theme]{ggplot2::theme()}} via \code{\link[=theme_calendar]{theme_calendar()}}.}
}
\value{
A ggplot object.
}
\description{
Draws the calendar as a wall calendar: one facet per month (in member
order -- April first for the \verb{fy04_*} fiscal calendars), day cells in
a week grid, weeks top-down. With \code{data}, day cells are filled by the
aggregated value; without, a plain calendar (day numbers only).
}
\details{
\code{data} may be keyed by timeslice (day-level IDs directly; finer IDs
-- e.g. hours of \code{d365_h24} -- roll up into their day cell) or by
datetime (mapped through \code{\link[=datetime_to_timeslice]{datetime_to_timeslice()}} on the day
layer). The key column is auto-detected (\code{timeslice}, else the first
POSIXct/Date column) unless \verb{key=} names it.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  # plain wall calendar for 2021
  calendar_wall_plot(calendar("m12_md365"), year = 2021)

  # daily data on a fiscal wall (April..March facets)
  cal <- calendar("fy04_d365")
  x <- data.frame(
    timeslice = S7::prop(cal, "leaftable")$timeslice,
    v = cumsum(stats::rnorm(365))
  )
  calendar_wall_plot(cal, x, z = "v", year = 2021)
}
}
