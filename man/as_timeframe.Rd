% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeframes.R
\name{as_timeframe}
\alias{as_timeframe}
\alias{as_timeframe.POSIXt}
\alias{as_timeframe.Date}
\alias{as_timeframe.default}
\title{Extract timeframe values from datetime vectors}
\usage{
as_timeframe(x, timeframe, format = c("numeric", "token"), ...)

\method{as_timeframe}{POSIXt}(x, timeframe, format = c("numeric", "token"), ...)

\method{as_timeframe}{Date}(x, timeframe, format = c("numeric", "token"), ...)

\method{as_timeframe}{default}(x, timeframe, format = c("numeric", "token"), ...)
}
\arguments{
\item{x}{A datetime vector (\code{Date}, \code{POSIXct}, or \code{POSIXlt}).}

\item{timeframe}{Character scalar naming the timeframe to extract;
one of \code{\link{CORE_TIMEFRAMES}}.}

\item{format}{\code{"numeric"} (default) for raw integer values, or \code{"token"}
for formatted strings (\code{"m01"}, \code{"h00"}, \code{"d365"}, \code{"MON"}, ...).}

\item{...}{Currently \code{week_start} is forwarded to \code{lubridate::wday()} for
\code{WDAY} and \code{MWEEK}.}
}
\value{
Integer/numeric vector for \code{format = "numeric"}, character vector
for \code{format = "token"}. Preserves \code{NA} positions.
}
\description{
Extracts a single timeframe component (\code{YEAR}, \code{MONTH}, \code{HOUR}, ...) from a
\code{Date}, \code{POSIXct}, or \code{POSIXlt} vector, optionally formatted as a token
string.
}
\details{
For datetime input (no calendar), \code{lubridate} defaults apply:
\itemize{
\item \code{WDAY} uses \code{getOption("lubridate.week.start", 7)} (1 = Sunday).
\item \code{MONTH}: 1 = January, 12 = December.
\item \code{HOUR}: 0-23.
\item \code{MWEEK}: calendar-grid week-of-month aligned to \code{WDAY}.
}
}
\examples{
library(lubridate)
dtm <- ymd_h("2020-03-15 14", tz = "UTC")
as_timeframe(dtm, "MONTH")                   # 3
as_timeframe(dtm, "YDAY", format = "token")  # "d075"
}
