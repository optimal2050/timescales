% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navigate.R
\name{calendar_navigate}
\alias{calendar_navigate}
\alias{calendar_children}
\alias{calendar_parents}
\alias{calendar_descendants}
\alias{calendar_ancestors}
\title{Navigate a Calendar hierarchy}
\usage{
calendar_children(calendar, timeframe, label, to = NULL)

calendar_parents(calendar, timeframe, label, to = NULL)

calendar_descendants(calendar, timeframe, label, to = NULL)

calendar_ancestors(calendar, timeframe, label, to = NULL)
}
\arguments{
\item{calendar}{A \link{Calendar}.}

\item{timeframe}{The timeframe \code{label} lives at.}

\item{label}{One or more labels at \code{timeframe}.}

\item{to}{Optional target timeframe; defaults to the adjacent one
(children/parents) or the full transitive range
(descendants/ancestors).}
}
\value{
\code{calendar_children()} / \code{calendar_parents()} return a
character vector; \code{calendar_descendants()} /
\code{calendar_ancestors()} a \code{data.frame(timeframe, label)}.
}
\description{
Labels related to \code{label} across timeframes — the time-side mirror of
\code{geoscales::geoscale_children()} and friends. Time levels nest
strictly, so these are exact partitions:
}
\details{
\itemize{
\item \code{calendar_children()} — one step finer (or at \code{to}).
\item \code{calendar_parents()} — one step coarser (or at \code{to}).
\item \code{calendar_descendants()} — all finer timeframes (down to \code{to}).
\item \code{calendar_ancestors()} — all coarser timeframes (up to \code{to}).
}
}
\examples{
cal <- calendar("q4_h24")
calendar_children(cal, "QUARTER", "Q1")
calendar_parents(cal, "HOUR", "h00")
}
