% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calendar-construct.R
\name{calendar_from_leaftable}
\alias{calendar_from_leaftable}
\title{Build a Calendar from a flat table of leaf timeslices}
\usage{
calendar_from_leaftable(
  leaftable,
  timeframes,
  members = NULL,
  name = "",
  desc = "",
  year_start = list(month = 1L, day = 1L),
  utc_offset_minutes = 0L,
  year_fraction = 1,
  ...
)
}
\arguments{
\item{leaftable}{A \code{data.frame} with one row per leaf timeslice. Must contain:
\itemize{
\item one column per timeframe named in \code{timeframes}
\item \code{share} — numeric > 0; sums to \code{year_fraction}
\item \code{weight} — numeric >= 0; user-defined importance weight
\item (optional) \code{timeslice} — unique character ID; auto-generated if missing
}}

\item{timeframes}{Ordered character vector of timeframe names (coarsest
first). Each must appear as a column in \code{leaftable}.}

\item{members}{Optional named list giving the full ordered member set per
timeframe. If \code{NULL}, derived from \code{unique(leaftable[[tf]])} in
first-appearance order.}

\item{name, desc}{Calendar name and free-text description.}

\item{year_start}{\code{list(month = , day = )}; defaults to January 1.}

\item{utc_offset_minutes}{Integer minutes; model-local time = UTC + offset.}

\item{year_fraction}{Fraction of a year covered by \code{sum(leaftable$share)}.
Defaults to \code{1}.}

\item{...}{Additional named entries appended to \code{meta} (free-form).}
}
\value{
A \code{\link{Calendar}} object.
}
\description{
This is the most general way to construct a \code{\link{Calendar}}: provide the leaf
timeslices directly as a \code{data.frame}, name the timeframe columns, and
optionally pin down the per-timeframe vocabulary and model-level metadata.
}
\examples{
# A trivial monthly calendar with weights = days in month (non-leap year)
df <- data.frame(
  MONTH  = sprintf("m\%02d", 1:12),
  share  = c(31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31) / 365,
  weight = c(31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)
)
cal <- calendar_from_leaftable(df, timeframes = "MONTH", name = "m12")
cal
}
