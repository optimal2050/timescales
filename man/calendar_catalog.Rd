% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{calendar_catalog}
\alias{calendar_catalog}
\title{The calendar catalog}
\usage{
calendar_catalog()
}
\value{
A \code{data.frame} with one row per catalog entry: \code{id}, \code{tokens}
(\code{+}-joined), \code{timeframes} (\code{/}-joined), \code{n_slices}, \code{coverage}
(\code{complete}/\code{truncated}/\code{representative}), \code{regularity}
(\code{regular}/\code{irregular}), and a generated \code{desc}.
}
\description{
Enumerates the named calendar designs \code{\link[=calendar]{calendar()}} knows about — the
curated set ported from the predecessor \code{timeslices} package, all with
duration-proportional shares. Every id here can be built with
\code{calendar(id)}; the same objects are shipped pre-built in the
\code{\link{calendars}} dataset.
}
\examples{
cat_df <- calendar_catalog()
head(cat_df)
subset(cat_df, coverage == "complete")
}
