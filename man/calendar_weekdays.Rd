% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wall.R
\name{calendar_weekdays}
\alias{calendar_weekdays}
\title{Weekdays of a calendar's day layer in a given model year}
\usage{
calendar_weekdays(x, year, week_start = "MON")
}
\arguments{
\item{x}{A \code{\link{Calendar}} with a day-resolution timeframe
(\code{YDAY} or \code{MDAY}). Sub-daily timeframes are collapsed to the day
layer via \code{\link[=prune_calendar]{prune_calendar()}}.}

\item{year}{Integer scalar model year.}

\item{week_start}{Weekday the week starts on: one of
\code{"MON"}..\code{"SUN"} (default \code{"MON"}, ISO).}
}
\value{
A \code{data.frame}, one row per day of the day layer, in calendar
order: \code{timeslice} (day-level node ID), \code{date} (\code{Date}, \code{NA} where
the stylized day has no real date), \code{MONTH} (facet key: the
calendar's MONTH members when present, else the Gregorian
\code{m01..m12} template; fiscal-ordered under a nontrivial
\code{year_start}), \code{mday} (day-of-month number), \code{wday} (factor,
vocabulary rotated to \code{week_start}), \code{wrow} (week-of-month row,
1 = first week, breaking on \code{week_start}), and \code{wyear}
(week-of-year, week 1 = the week containing the year anchor).
}
\description{
Maps every day of the calendar's day layer (its \code{YDAY} or \code{MDAY}
timeframe) onto the real dates of one model year and reports the
weekday structure: weekday, week-of-month row, and an anchored
week-of-year index. The model year honours \code{meta$year_start} (so for
the \verb{fy04_*} fiscal calendars it runs April..March and \code{wyear} counts
fiscal weeks from April 1) and \code{meta$utc_offset_minutes}.
}
\details{
Stylized days with no real date in that year (\code{m12_md360}'s day 31,
the \code{d366} label in a non-leap year) get \code{date = NA} with one warning.
}
\examples{
wd <- calendar_weekdays(calendar("m12_md365"), 2021)
head(wd)
# fiscal weeks: April 1 opens week 1
head(calendar_weekdays(calendar("fy04_d365"), 2021))
}
