% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base-calendar.R
\name{calendar_at_level}
\alias{calendar_at_level}
\title{Derive a coarser calendar by truncating the hierarchy at a timeframe}
\usage{
calendar_at_level(calendar, timeframe)
}
\arguments{
\item{calendar}{A \code{\link{Calendar}}.}

\item{timeframe}{One of \code{calendar}'s timeframes, or \code{"ANNUAL"} for the
whole-year root.}
}
\value{
A \code{\link{Calendar}} whose hierarchy stops at \code{timeframe}.
}
\description{
Aggregates a calendar to one of its own timeframe levels: leaves are
grouped by the timeframes down to (and including) \code{timeframe}, and their
\code{share}/\code{weight} are summed. \code{timeframe = "ANNUAL"} returns the implicit
whole-year root — a one-slice calendar (the root is named \code{ANNUAL}, never
\code{YEAR}, which is reserved for the Gregorian-year axis).
}
\details{
Together with \code{\link[=recast]{recast()}}'s acceptance of a timeframe name for \verb{to=},
this covers within-calendar aggregation (e.g. \code{q4_h24 -> q4}) without
constructing a second calendar by hand.
}
\examples{
cal <- calendar_build("q4", "h24")
calendar_at_level(cal, "QUARTER")   # 4 slices, shares summed over hours
calendar_at_level(cal, "ANNUAL")    # 1 slice covering the year
}
