% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wall.R
\name{calendar_wall_layout}
\alias{calendar_wall_layout}
\title{Wall-calendar tile layout}
\usage{
calendar_wall_layout(
  x,
  year = NULL,
  arrange = c("weekday", "sequence"),
  week_start = "MON"
)
}
\arguments{
\item{x}{A \code{\link{Calendar}} with a day-resolution timeframe
(\code{YDAY} or \code{MDAY}). Sub-daily timeframes are collapsed to the day
layer via \code{\link[=prune_calendar]{prune_calendar()}}.}

\item{year}{Integer model year (required for the weekday
arrangement; \code{NULL} falls back to \code{"sequence"} with a message).}

\item{arrange}{\code{"weekday"} or \code{"sequence"} (see above).}

\item{week_start}{Weekday the week starts on: one of
\code{"MON"}..\code{"SUN"} (default \code{"MON"}, ISO).}
}
\value{
A \code{data.frame}: \code{timeslice}, \code{MONTH} (factor in member
order), \code{label} (day-of-month number), \code{col}, \code{row}, \code{wday}
(factor, \code{NA} in sequence mode), \code{date} (\code{NA} in sequence mode).
}
\description{
The layout worker behind \code{\link[=calendar_wall_plot]{calendar_wall_plot()}}: one row per day
cell with facet key and grid position. Two arrangements:
}
\details{
\itemize{
\item \code{arrange = "weekday"} -- a true wall calendar: columns are weekdays
(starting at \code{week_start}), rows are weeks of the month. Needs
\code{year} (weekdays are year-specific); when \code{year} is \code{NULL} it FALLS
BACK to \code{"sequence"} with a message.
\item \code{arrange = "sequence"} -- year-free: days flow left-to-right in
fixed 7-wide rows, no weekday meaning.
}
}
\examples{
head(calendar_wall_layout(calendar("m12_md365"), year = 2021))
head(calendar_wall_layout(calendar("m12_md365")))  # sequence fallback
}
