% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calendar-build.R
\name{calendar_build}
\alias{calendar_build}
\alias{calendar}
\title{Build a Calendar from token names}
\usage{
calendar_build(
  ...,
  name = NULL,
  desc = "",
  year_start = list(month = 1L, day = 1L),
  utc_offset_minutes = 0L,
  year_fraction = 1
)

calendar(name, ...)
}
\arguments{
\item{...}{Passed through to \code{\link[=calendar_build]{calendar_build()}} (\code{year_start},
\code{utc_offset_minutes}, \code{year_fraction}, \code{desc}).}

\item{name}{Character scalar — a token-style calendar name (\code{"d365"},
\code{"d365_h24"}, \code{"m12_h24"}, \code{"q4_h24"}, ...).}

\item{desc}{Free-text description.}

\item{year_start}{\code{list(month = , day = )}; defaults to January 1.}

\item{utc_offset_minutes}{Integer minutes; defaults to 0.}

\item{year_fraction}{Year fraction covered. Defaults to 1.}
}
\value{
A \code{\link{Calendar}}.

A \code{\link{Calendar}}.
}
\description{
Declarative constructor: name the timeframe vocabulary tokens in coarsest-
to-finest order. The Cartesian product of their labels becomes the leaf
table; each leaf's share is the product of its tokens' shares, scaled to
\code{year_fraction}.

Convenience shortcut. Names listed in \code{\link[=calendar_catalog]{calendar_catalog()}} build the
catalog design (with \code{coverage}/\code{regularity} metadata attached; the
\verb{m12_md*} family uses a dedicated ragged month/day builder). Any other
name is parsed as \verb{_}-joined tokens and dispatched to
\code{\link[=calendar_build]{calendar_build()}}. The leading \code{y_} prefix (year-qualified) is
currently stripped and recorded in \code{meta$year_qualified} — full
year-prefix semantics arrive in a later phase.
}
\examples{
cal <- calendar_build("d365", "h24", name = "d365_h24")
cal

cal2 <- calendar_build("m12", "h24")
cal2
calendar("d365_h24")
calendar("m12_h24")
}
