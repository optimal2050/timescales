% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\name{CALENDAR_RULES}
\alias{CALENDAR_RULES}
\title{Supported aggregation rules}
\format{
A character vector of length 7.
}
\usage{
CALENDAR_RULES
}
\description{
Each rule defines behaviour in \strong{both} directions of the instant grid, so
aggregation and disaggregation are one operation (the geoscales split of
extensive vs intensive quantities):
}
\details{
\describe{
\item{\code{weighted_mean}}{Down: copy. Up: mean weighted by the declared
\code{leaves$share} of each source timeslice. For intensive quantities
(load, price, efficiency). The default.}
\item{\code{sum}}{Down: split equally across the timeslice's grid instants.
Up: sum. Conserves totals; for extensive quantities (energy, cost).}
\item{\code{mean}}{Down: copy. Up: plain mean over instants — a time-weighted
mean, since the grid is uniform. Differs from \code{weighted_mean} exactly
when declared shares differ from real-time coverage.}
\item{\code{copy}}{Down: copy. Up: the common value, erroring if it is not
constant. For timeslice-invariant scalars.}
\item{\code{sd}}{Up: standard deviation over instants — the spread of the
fine signal within each target timeslice. Aggregation-only; going finer it
degenerates to 0/NA.}
}

\item{\code{share}}{Share within parent: each source timeslice's value
divided by the total over its parent timeslice. Unlike every other
rule the result stays keyed by the \strong{source} calendar's timeslices
-- \code{to} (or \verb{parent=}) names the parent calendar or timeframe -- so
it cannot be mixed with other rules in one call. For building
profile shares and distribution keys; requires the source
timeslices to nest within the parent's. The two share rules mix
freely with each other, being one computation.}
\item{\code{logshare}}{The same computation as \code{share} -- the values ARE
shares -- but figures draw it on a fixed log10 percent scale
(0.01\%..100\%), where \code{share} gets a fixed linear 0..1 scale. Use
it when sibling counts differ by orders of magnitude and the
linear scale flattens the crowded groups.}
}
\examples{
CALENDAR_RULES
}
